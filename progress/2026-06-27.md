# AgentFlow — 2026-06-27

## Active plan

- [plans/post-mvp/18-manifest-clean-versioning.md](../plans/post-mvp/18-manifest-clean-versioning.md) — **In progress**

## Branch ownership

| Branch | Worktree / location | PR | Status |
|--------|---------------------|-----|--------|
| `feat/m18-manifest` | `/Users/abhinand/Desktop/Misc/agentFlow` | [#14](https://github.com/Abhinand20/agentFlow/pull/14) | active |

Notes: Per-source build manifest, `af clean`, `af versions list|diff|status`, `build --prune`.

## Focus

- Milestone: **M18 — Manifest, Clean & Versioning**
- Per-source manifest under `.cursor/.agentflow/manifests/<slug>.json`
- Source-scoped `af clean`; hash-only capped history for `af versions`

## Completed

- [x] `internal/manifest` — schema, Build, Load/LoadAll, Diff, DriftCheck, Slug
- [x] `af build` — writes per-source manifest; `--no-manifest`, `--prune`, `--force`
- [x] `af clean <file>` — source-scoped delete; `--all`, drift + cross-source guards
- [x] `af versions list|diff|status` — grouped history and drift reporting
- [x] Unit + CLI tests for manifest, clean isolation, prune, versions

## Verification

```bash
go build ./... && go test ./...
go test ./internal/manifest/... -v
go test ./cmd/af/... -v
```

## Next session

- Open PR for M18; merge after review
- **M7** — Claude binding (`af build --target claude-code`)
- Wire manifest path for Claude target root (`.claude/.agentflow/`)
